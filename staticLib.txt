1.生成Lib文件：属性->常规->项目默认值-配置类型->静态库；确定后生成项目，Debug文件下会生成ProjectName.Lib静态库文件

2.将静态库文件和头文件打包到一个文件夹中，以便配置

3.新建工程，在属性中配置：
	包含目录设置为 Includes文件目录		用于找头文件的位置，以便引入头文件
	库目录设置为   Libs文件目录		用于找Lib文件的位置
	链接器->附加依赖项中添加 ProjectName.Lib 进去即可

另：也可以不在连接器中添加，直接在程序中加上这句话：#pragma comment(lib, “name.lib”)） 也可以引入